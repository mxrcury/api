version: "3.8"

networks:
  mavy:
    driver: bridge

services:
  api:
    container_name: api
    build:
      context: .
      dockerfile: api.Dockerfile
    ports:
      - ${PORT}:${PORT}
    environment:
      PORT: ${PORT}
    networks:
      - mavy
    restart: always
